import * as actions from './commonActions';
import * as actionTypes from './commonActionTypes';
import * as reducers from './commonReducer';
import * as selectors from './commonSelectors';

const NAME = 'common';

export default { actionTypes, actions, reducers, selectors, NAME };
